## EquationFunction

### Methods

- clear() — EquationFunction
- copy() — EquationFunction
- editAsText() — Text
- findElement(elementType) — RangeElement
- findElement(elementType, from) — RangeElement
- findText(searchPattern) — RangeElement
- findText(searchPattern, from) — RangeElement
- getAttributes() — Object
- getChild(childIndex) — Element
- getChildIndex(child) — Integer
- getCode() — String
- getLinkUrl() — String
- getNextSibling() — Element
- getNumChildren() — Integer
- getParent() — ContainerElement
- getPreviousSibling() — Element
- getText() — String
- getTextAlignment() — TextAlignment
- getType() — ElementType
- isAtDocumentEnd() — Boolean
- merge() — EquationFunction
- removeFromParent() — EquationFunction
- replaceText(searchPattern, replacement) — Element
- setAttributes(attributes) — EquationFunction
- setLinkUrl(url) — EquationFunction
- setTextAlignment(textAlignment) — EquationFunction
