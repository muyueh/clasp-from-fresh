## DataSourcePivotTable

### Methods

- addColumnGroup(columnName) — PivotGroup
- addFilter(columnName, filterCriteria) — PivotFilter
- addPivotValue(columnName) — PivotValue
- addPivotValue(columnName, summarizeFunction) — PivotValue
- addRowGroup(columnName) — PivotGroup
- asPivotTable() — PivotTable
- cancelDataRefresh() — DataSourcePivotTable
- forceRefreshData() — DataSourcePivotTable
- getDataSource() — DataSource
- getStatus() — DataExecutionStatus
- refreshData() — DataSourcePivotTable
- waitForCompletion(timeoutInSeconds) — DataExecutionStatus
