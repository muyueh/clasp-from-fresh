## DateItem

### Methods

- createResponse(response) — ItemResponse
- duplicate() — DateItem
- getGeneralFeedback() — QuizFeedback
- getHelpText() — String
- getId() — Integer
- getIndex() — Integer
- getPoints() — Integer
- getTitle() — String
- getType() — ItemType
- includesYear() — Boolean
- isRequired() — Boolean
- setGeneralFeedback(feedback) — DateItem
- setHelpText(text) — DateItem
- setIncludesYear(enableYear) — DateItem
- setPoints(points) — DateItem
- setRequired(enabled) — DateItem
- setTitle(title) — DateItem
