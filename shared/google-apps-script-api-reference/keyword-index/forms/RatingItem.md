## RatingItem

### Methods

- createResponse(response) — ItemResponse
- duplicate() — RatingItem
- getGeneralFeedback() — QuizFeedback
- getHelpText() — String
- getId() — Integer
- getIndex() — Integer
- getPoints() — Integer
- getRatingIcon() — RatingIconType
- getRatingScaleLevel() — Integer
- getTitle() — String
- getType() — ItemType
- isRequired() — Boolean
- setGeneralFeedback(feedback) — RatingItem
- setHelpText(text) — RatingItem
- setPoints(points) — RatingItem
- setRatingIcon(ratingIcon) — RatingItem
- setRatingScaleLevel(ratingScaleLevel) — RatingItem
- setRequired(enabled) — RatingItem
- setTitle(title) — RatingItem
