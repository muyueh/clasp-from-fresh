## RecurrenceRule

### Methods

- addDailyExclusion() — RecurrenceRule
- addDailyRule() — RecurrenceRule
- addDate(date) — EventRecurrence
- addDateExclusion(date) — EventRecurrence
- addMonthlyExclusion() — RecurrenceRule
- addMonthlyRule() — RecurrenceRule
- addWeeklyExclusion() — RecurrenceRule
- addWeeklyRule() — RecurrenceRule
- addYearlyExclusion() — RecurrenceRule
- addYearlyRule() — RecurrenceRule
- interval(interval) — RecurrenceRule
- onlyInMonth(month) — RecurrenceRule
- onlyInMonths(months) — RecurrenceRule
- onlyOnMonthDay(day) — RecurrenceRule
- onlyOnMonthDays(days) — RecurrenceRule
- onlyOnWeek(week) — RecurrenceRule
- onlyOnWeekday(day) — RecurrenceRule
- onlyOnWeekdays(days) — RecurrenceRule
- onlyOnWeeks(weeks) — RecurrenceRule
- onlyOnYearDay(day) — RecurrenceRule
- onlyOnYearDays(days) — RecurrenceRule
- setTimeZone(timeZone) — EventRecurrence
- times(times) — RecurrenceRule
- until(endDate) — RecurrenceRule
- weekStartsOn(day) — RecurrenceRule
