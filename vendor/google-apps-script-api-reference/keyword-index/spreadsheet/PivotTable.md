## PivotTable

### Methods

- addCalculatedPivotValue(name, formula) — PivotValue
- addColumnGroup(sourceDataColumn) — PivotGroup
- addFilter(sourceDataColumn, filterCriteria) — PivotFilter
- addPivotValue(sourceDataColumn, summarizeFunction) — PivotValue
- addRowGroup(sourceDataColumn) — PivotGroup
- asDataSourcePivotTable() — DataSourcePivotTable
- getAnchorCell() — Range
- getColumnGroups() — PivotGroup[]
- getFilters() — PivotFilter[]
- getPivotValues() — PivotValue[]
- getRowGroups() — PivotGroup[]
- getSourceDataRange() — Range
- getValuesDisplayOrientation() — Dimension
- remove() — void
- setValuesDisplayOrientation(dimension) — PivotTable
