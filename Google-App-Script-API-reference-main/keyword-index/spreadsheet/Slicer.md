## Slicer

### Methods

- getBackgroundColorObject() — Color
- getColumnPosition() — Integer
- getContainerInfo() — ContainerInfo
- getFilterCriteria() — FilterCriteria
- getRange() — Range
- getTitle() — String
- getTitleHorizontalAlignment() — String
- getTitleTextStyle() — TextStyle
- isAppliedToPivotTables() — Boolean
- remove() — void
- setApplyToPivotTables(applyToPivotTables) — Slicer
- setBackgroundColor(color) — Slicer
- setBackgroundColorObject(color) — Slicer
- setColumnFilterCriteria(columnPosition, filterCriteria) — Slicer
- setPosition(anchorRowPos, anchorColPos, offsetX, offsetY) — Slicer
- setRange(rangeApi) — Slicer
- setTitle(title) — Slicer
- setTitleHorizontalAlignment(horizontalAlignment) — Slicer
- setTitleTextStyle(textStyle) — Slicer
