## PivotGroup

### Methods

- addManualGroupingRule(groupName, groupMembers) — PivotGroup
- areLabelsRepeated() — Boolean
- clearGroupingRule() — PivotGroup
- clearSort() — PivotGroup
- getDateTimeGroupingRule() — DateTimeGroupingRule
- getDimension() — Dimension
- getGroupLimit() — PivotGroupLimit
- getIndex() — Integer
- getPivotTable() — PivotTable
- getSourceDataColumn() — Integer
- getSourceDataSourceColumn() — DataSourceColumn
- hideRepeatedLabels() — PivotGroup
- isSortAscending() — Boolean
- moveToIndex(index) — PivotGroup
- remove() — void
- removeManualGroupingRule(groupName) — PivotGroup
- resetDisplayName() — PivotGroup
- setDateTimeGroupingRule(dateTimeGroupingRuleType) — PivotGroup
- setDisplayName(name) — PivotGroup
- setGroupLimit(countLimit) — PivotGroup
- setHistogramGroupingRule(minValue, maxValue, intervalSize) — PivotGroup
- showRepeatedLabels() — PivotGroup
- showTotals(showTotals) — PivotGroup
- sortAscending() — PivotGroup
- sortBy(value, oppositeGroupValues) — PivotGroup
- sortDescending() — PivotGroup
- totalsAreShown() — Boolean
